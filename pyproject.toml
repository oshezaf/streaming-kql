[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "streaming-kql"
dynamic = ["version"]
description = "Evaluate KQL over independent events with per-event row sets and bounded tables, in pure Python."
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "Ofer Shezaf" }]
keywords = ["kql", "kusto", "streaming", "log", "transform", "asim", "sentinel", "azure-monitor"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Logging",
    "Topic :: Text Processing :: Filters",
]
dependencies = ["lark>=1.1"]

[project.optional-dependencies]
dev = [
    "pytest>=7",
    "pytest-cov",
    "pyyaml>=6",
    "hypothesis>=6",
    "ruff>=0.5",
    "mypy>=1.8",
]

[project.urls]
Homepage = "https://github.com/oshezaf/streaming-kql"
Repository = "https://github.com/oshezaf/streaming-kql"
Documentation = "https://github.com/oshezaf/streaming-kql/tree/main/docs"
Changelog = "https://github.com/oshezaf/streaming-kql/blob/main/CHANGELOG.md"
Issues = "https://github.com/oshezaf/streaming-kql/issues"

[tool.hatch.version]
path = "streaming_kql/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["streaming_kql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.10"
warn_unused_ignores = true
disallow_untyped_defs = true
packages = ["streaming_kql"]
